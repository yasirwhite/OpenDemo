# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-electron
dist-ssr
*.local
.env

# Native helper build outputs
/electron/native/wgc-capture/build/
/electron/native/screencapturekit/build/
/electron/native/screencapturekit/.build/
/electron/native/screencapturekit/.swiftpm/
/electron/native/bin/

# Native macOS generated files
DerivedData/
*.xcuserstate
xcuserdata/

# Editor directories and files
.vscode/*
.zed/
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
release/**
*.kiro/
.claude/
# npx electron-builder --mac --win

# Playwright
test-results
playwright-report/

# AutoScreen Outputs
recordings/


# Vitest browser mode screenshots
__screenshots__/

# shell files
/shell.sh
# Nix
result
result-*
.direnv/

#kilocode
.kilo/

#others

**/*.import

# Auto-caption model + ORT wasm — regenerated at build by scripts/fetch-caption-model.mjs
/caption-assets/

# Rendered video. Cinematic output regenerates from a config:
#   node src/lib/cinematic3d/render.mjs <config.json>
*.mp4

# ...except the demo fixture the example config and tests render against.
!examples/login-demo.mp4

# Reference films — fetched on demand, never committed. See references/README.md.
references/media/

# Rendered output and intermediate frame dumps.
recordings_3d/
**/.cinematic3d-frames/
**/.cinematic3d-config.json

# Working cinematic configs live in the repo but are not committed. The shared
# examples/ directory at the repo root holds every committed example config.
src/lib/cinematic3d/configs/*.json

# scratch for demo builds (recordings, 3D passes, mock apps)
.demo-build/

# gallery site build output — CI rebuilds this from site/build.mjs on every deploy
_site/

# the author's working rulebook for generated demo videos — read before planning
# a video, not after the first render
VIDEO_PRINCIPLES.md

# audio beds live on the author's machine, never in the repo
*.mp3
*.wav
*.m4a
